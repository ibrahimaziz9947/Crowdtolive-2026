import { NotQualifiedView } from "@/features/registration-qualification";

export const metadata = {
  title: "Not Qualified | CrowdToLive",
  description: "Registration qualification result.",
};

export default function NotQualifiedPage() {
  return <NotQualifiedView />;
}

